# Gulaab docs

Small Next.js app for previewing `@gulaab/ui`.

Run it from the repo root:

```bash
pnpm dev
```

Or from this app:

```bash
pnpm --filter docs dev
```

The main page lives in `app/page.tsx`.
